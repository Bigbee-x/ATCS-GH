# ATCS-GH — Python dependencies
# ═════════════════════════════
# Install with:  python -m pip install -r requirements.txt
# Tested with Python 3.11 on macOS (Apple Silicon; torch picks the MPS backend).
#
# eclipse-sumo bundles the SUMO binaries AND the traci/sumolib tools —
# ai/traffic_env.py / ai/corridor_env.py bootstrap SUMO_HOME from the installed
# package automatically, so no separate SUMO install or env var is needed.

torch>=2.3          # Double-DQN networks (MPS on Apple Silicon automatically)
numpy>=1.26         # numerics
websockets>=12.0    # SUMO ↔ Godot visualizer bridge
flask>=3.0          # metrics dashboard (http://127.0.0.1:5050)
eclipse-sumo>=1.20  # SUMO simulator + bundled TraCI/sumolib tools
